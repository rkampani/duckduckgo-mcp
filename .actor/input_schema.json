{
  "title": "DuckDuckGo MCP Server Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "mode": {
      "title": "Server Mode",
      "type": "string",
      "description": "Mode to run the MCP server: stdio for local integration, http for remote access",
      "enum": ["stdio", "http"],
      "default": "http",
      "editor": "select"
    },
    "port": {
      "title": "HTTP Port",
      "type": "integer",
      "description": "Port number for HTTP mode (only used when mode is 'http')",
      "default": 3000,
      "minimum": 1024,
      "maximum": 65535,
      "editor": "number"
    },
    "host": {
      "title": "HTTP Host",
      "type": "string",
      "description": "Host to bind HTTP server (use 0.0.0.0 for all interfaces)",
      "default": "0.0.0.0",
      "editor": "textfield"
    },
    "maxResultsDefault": {
      "title": "Default Max Results",
      "type": "integer",
      "description": "Default number of search results to return if not specified",
      "default": 10,
      "minimum": 1,
      "maximum": 50,
      "editor": "number"
    },
    "searchRateLimit": {
      "title": "Search Rate Limit (per minute)",
      "type": "integer",
      "description": "Maximum number of search requests allowed per minute",
      "default": 30,
      "minimum": 1,
      "maximum": 100,
      "editor": "number"
    },
    "fetchRateLimit": {
      "title": "Fetch Rate Limit (per minute)",
      "type": "integer",
      "description": "Maximum number of content fetch requests allowed per minute",
      "default": 20,
      "minimum": 1,
      "maximum": 100,
      "editor": "number"
    },
    "enableLogging": {
      "title": "Enable Detailed Logging",
      "type": "boolean",
      "description": "Enable detailed logging for debugging purposes",
      "default": true,
      "editor": "checkbox"
    },
    "safeModeDefault": {
      "title": "Safe Search Default",
      "type": "boolean",
      "description": "Enable safe search by default for all queries",
      "default": true,
      "editor": "checkbox"
    }
  },
  "required": []
}
